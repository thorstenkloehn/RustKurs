## Rust Datentypen einfach erklärt
* Rust ist eine statisch typisierte Sprache
## Skalare Typen
### Ein skalarer Typ repräsentiert einen einzigen Wert. Die wichtigsten numerischen Typen sind:
#### Ganzzahlen (Integers): Zahlen ohne Komma.
##### Ganzzahlen (Integers): Zahlen ohne Komma.
* Mit Vorzeichen (i für signed): Können positiv und negativ sein (z. B. -5, 12). Standard ist i32.
* Ohne Vorzeichen (u für unsigned): Können nur 0 oder positiv sein (z. B. 5). Weil sie kein Minuszeichen speichern müssen, passen größere positive Zahlen hinein.
#### Fließkommazahlen (Floats): Zahlen mit Nachkommastellen (Dezimalzahlen). Standard ist
* f64
### Was bedeuten die Zahlen (z. B. 8, 32, 64)?

Wichtig für Anfänger: Verschwende am Anfang keine Zeit mit "Mikro-Optimierung" des Speichers. Nutze einfach die * * * * Rust-Standards (i32 und f64). Heutige Computer haben mehr als genug Speicher.
* Statische Typisierung: Rust muss jeden Datentyp vor dem Start des Programms kennen.

* Typinferenz: Rust erkennt den Typ oft automatisch anhand des zugewiesenen Wertes.

* Zusammengesetzte Typen (Vorschau): Können im Gegensatz zu skalaren Typen mehrere Werte wie ein "Behälter" aufnehmen.

Speicher-Eselsbrücke
* i = Integer (Ganzzahl mit Vorzeichen, kann ins Ins Negative gehen).

* u = Unsigned (Vorzeichenlos, Unmöglich negativ).

* Standard-Regel: Wenn du eine normale Zahl brauchst, nimm einfach i32. Wenn du eine Kommazahl brauchst, nimm f64.

Das Wichtigste in Kürze
* Explizite Typisierung: Standardmäßig macht Rust aus ganzen Zahlen ein i32. Willst du das ändern, hängst du einen Doppelpunkt und den Typ an den Variablennamen (z. B. let x: i8 = -100;).
* Wertebereich beachten: Jeder Typ hat Grenzen. Passt eine Zahl nicht in den Speicherplatz (z. B. 400 in ein u8), streikt der Compiler und das Programm startet erst gar nicht.
* Alternative Schreibweise (Suffix): Du kannst den Typ auch direkt ohne Leerzeichen an die Zahl klatschen (z. B. let x = 20i8;). Das bewirkt genau dasselbe, wird aber seltener genutzt.
## Merkzettel: Rust Integer (Ganzzahlen) im Überblick
* i (signed / mit Vorzeichen): Kann negative und positive Zahlen speichern.
* u (unsigned / ohne Vorzeichen): Kann nur positive Zahlen und 0 speichern. Vorteil: Da kein Minuszeichen gespeichert werden muss, verdoppelt sich die Reichweite im positiven Bereich.
## Typen, Speicherplatz und ihre Grenzen
SpeichergrößeGültiger Zahlenbereich (Grenzen)Beispiel für Fehleri81 Byte (8 Bit)-128 bis 127let x: i8 = -210; (zu klein)u81 Byte (8 Bit)0 bis 255let x: u8 = -15; (kein Minus erlaubt)i162 Bytes (16 Bit)-32.768 bis 32.767let x: i16 = 64000; (zu groß)u162 Bytes (16 Bit)0 bis 65.535let x: u16 = 66000; (zu groß)i324 Bytes (32 Bit)ca. -2,14 Mrd. bis +2,14 Mrd.(Standardtyp in Rust)u324 Bytes (32 Bit)0 bis ca. 4,29 Mrd.let x: u32 = -1; (kein Minus erlaubt)

Merke für die Praxis: Wenn eine Zahl den zugewiesenen Bereich sprengt, bricht Rust den Kompiliervorgang sofort mit einer Fehlermeldung ab. Das schützt dich vor Fehlern im fertigen Programm!

Das Wichtigste in Kürze

* Große Zahlen im Code (wie 6000000) sind schwer zu lesen. Rust erlaubt es dir, Unterstriche (_) als visuelle Trennzeichen in Zahlen einzufügen (wie das Tausendertrennzeichen in der Mathematik).

* Dem Compiler ist es egal: Rust ignoriert die Unterstriche beim Kompilieren komplett.

* Flexibel: Du kannst so viele Unterstriche setzen, wo du willst.

* Best Practice: Sinnvoll ist es, Zahlen in 3er-Blöcke aufzuteilen (z. B. 6_000_000).

* Eselsbrücke: Ersetze das mathematische Tausender-Komma (oder den Punkt) im Code einfach durch einen Unterstrich _. Das erhöht die Lesbarkeit sofort und verhindert Tippfehler bei vielen Nullen!

## Das Wichtigste in Kürze

Während Typen wie i32 oder u8 auf jedem Computer immer exakt gleich groß sind, passen sich isize und usize automatisch an den Computer an, auf dem das Programm gerade läuft.
* Die Anpassung: Auf einem 64-Bit-Computer werden sie zu 64-Bit-Typen (i64 / u64). Auf einem 32-Bit-Computer werden sie zu 32-Bit-Typen (i32 / u32).
* Der Zweck: Sie dienen als "Spitznamen" (Aliase) für den nativen Typ des Systems. Das macht den Code extrem flexibel und spart automatisch Speicher auf älteren Systemen, während es auf modernen Computern die maximale Leistung herausholt.
Wofür braucht man das? In der Praxis nutzt man usize vor allem, wenn man Dinge zählt, die mit der Speichergröße des Computers zu tun haben – zum Beispiel, um die Länge von Listen (Arrays/Vektoren) zu bestimmen oder Elemente darin über einen Index anzusprechen.

## Was ist ein String-Literal?
Ein Text in doppelten Anführungszeichen (z. B. "Hello World") ist in Rust technisch gesehen ein String-Literal. Der Compiler kennt diesen Text schon beim Verharmlosen (Kompilieren), weil er fest im Quellcode geschrieben steht. Dynamische Texte (z. B. Benutzereingaben) funktionieren später anders.
## Maskierung (Escaping) & Sonderzeichen
Manche Zeichen haben in Rust eine Sonderfunktion. Sie beginnen immer mit einem Backslash (\). Möchtest du diese Zeichen wörtlich als Text drucken, musst du sie mit einem zusätzlichen Backslash "maskieren" (escapen).

* \n erzeugt eine neue Zeile.

* \t erzeugt einen Tabulator-Abstand.

* \" druckt ein echtes Anführungszeichen, ohne dass Rust denkt, der String ist vorbei.

* \\ druckt einen einzelnen, echten Backslash (wichtig z. B. bei Windows-Dateipfaden).

Die Rettung: Raw Strings
Wenn du viele Backslashes oder Pfade hast, wird der Code durch das ständige Maskieren (\\) unleserlich. Die Lösung ist ein Raw String (Roh-Zeichenkette). Setze einfach ein r vor die Anführungszeichen. Rust ignoriert dann alle Sonderfunktionen und druckt alles exakt so, wie es da steht.

1. Was ist eine Methode?
Eine Methode ist im Grunde eine Funktion, die fest an einen bestimmten Wert oder Datentyp gebunden ist. Sie ist wie ein Befehl, den man direkt auf eine Variable anwendet.

* Funktion / Makro: Steht für sich alleine (z. B. println!(...)).

* Methode: Klebt immer mit einem Punkt an einer Variablen (z. B. text.trim()).

* Eingabe (Argument): Was in die Klammern reinwandert.

* Ausgabe (Rückgabewert): Das Ergebnis, das am Ende rauskommt.

Ein Float (Fließkommazahl) stellt eine Dezimalzahl mit Nachkommastellen dar. In Rust sind Floats immer vorzeichenbehaftet (sie können also positive und negative Werte haben). Es gibt keine "unsigned" (rein positiven) Floats.

fn main() {
    let pi: f64 = 3.14159;

    // 1. .floor() -> Abrunden (zum "Boden")
    println!("{}", pi.floor()); // Ausgabe: 3.0

    // 2. .ceil() -> Aufrunden (zur "Decke" / Ceiling)
    println!("{}", pi.ceil());  // Ausgabe: 4.0

    // 3. .round() -> Kaufmännisch runden (zur nächsten ganzen Zahl)
    println!("{}", pi.round()); // Ausgabe: 3.0 (da .14 näher an 3 ist)
    
    let kaffee_preis: f64 = 3.75;
    println!("{}", kaffee_preis.round()); // Ausgabe: 4.0 (da .75 näher an 4 ist)
}

Wenn du eine Kommazahl mit println! ausgibst, kannst du die Anzahl der angezeigten Nachkommastellen begrenzen. Das Zaubermittel dafür heißt Formatbezeichner (Format Speci)
Die Syntax: Innerhalb der geschweiften Klammern fügst du einen Doppelpunkt :, einen Punkt . und die gewünschte Anzahl an Stellen ein (z. B. :.2 für 2 Stellen).

* Wichtig: Der echte Wert in der Variable ändert sich dadurch nicht. Rust passt nur die optische Darstellung bei der Ausgabe an und rundet die Zahl dabei mathematisch korrekt.

* Zwei Wege: Das funktioniert sowohl, wenn die Variable direkt in den Klammern steht ({pi:.2}), als auch, wenn sie als Argument hinten dranhängt ({:.2}, pi).

Rust
fn main() {
    let pi = 3.14159265;

    // ---- Variante 1: Variable direkt in der Klammer (Kompakt) ----
    println!("Pi auf 2 Stellen: {pi:.2}"); // Ausgabe: 3.14
    println!("Pi auf 4 Stellen: {pi:.4}"); // Ausgabe: 3.1416 (automatisch aufgerundet!)

    // ---- Variante 2: Variable als Argument hinten dran (Klassisch) ----
    println!("Ausgabe auf 3 Stellen: {:.3}", pi); // Ausgabe: 3.142
    
    // Der Originalwert ist immer noch unverändert:
    println!("Originaler Wert: {}", pi); // Ausgabe: 3.14159265
}
💡 Eselsbrücke für die Praxis: Denk bei :.2 einfach an ein Gesicht mit Brille, das genau hinsieht: Der Doppelpunkt : startet das Tuning, der Punkt . trennt ab, und die 2 sagt, wie viele Ziffern übrig bleiben!

## Was ist Casting?
* Casting bedeutet, einen Wert von einem Datentyp in einen anderen umzuwandeln (z. B. eine Kommazahl in eine Ganzzahl). In Rust nutzt man dafür das Schlüsselwort as.
* 2. Wie funktioniert es?
* Du schreibst die ursprüngliche Variable, gefolgt von as und dem neuen Typ: variable as neuer_typ. Die originale Variable bleibt dabei völlig unverändert!

Praktische Code-Beispiele
Rust
fn main() {
    // ---- 1. Ganzzahl verkleinern (i32 zu i8) ----
    let miles_away = 50; // Rust denkt automatisch: i32
    let miles_i8 = miles_away as i8; // Sicher umgewandelt in i8

    // ---- 2. Float-Präzision ändern (f64 zu f32) ----
    let ungenaues_pi = 3.14159265f64; 
    let pi_f32 = ungenaues_pi as f32; // Jetzt als 32-Bit-Float

    // ---- 3. Kommazahl zu Ganzzahl (Achtung: Abschneiden!) ----
    let strecke_float = 100.99; // Standard: f64
    let strecke_int = strecke_float as i32; 
    
    println!("{}", strecke_int); // Ausgabe: 100 (Es wird NICHT gerundet, nur abgeschnitten!)
}
⚠️ Wichtiger Anfänger-Tipp: Rust rundet beim Casting von Float zu Integer niemals kaufmännisch auf! Selbst aus 99.99 as i32 wird gnadenlos die Ganzzahl 99. Möchtest du echtes Runden, nutze vorher die Methode .round().

## Was ist ein Boolean?
* Ein Boolean (benannt nach George Boole) stellt den Zustand der Wahrheit in der Programmierung dar. Er kennt exakt nur zwei Zustände: true (wahr) oder false (falsch). Damit lassen sich Ja/Nein-, An/Aus- oder Aktiv/Inaktiv-Szenarien perfekt abbilden.
* 2. Der Typ bool und sein Speicher
In Rust heißt dieser Typ kurz bool.

Der Speicher-Trick: Rein mathematisch würde 1 Bit (0 oder 1) reichen. Rust spendiert einem bool im Arbeitsspeicher aber 1 Byte (8 Bits). Warum? Moderne Computer-Architekturen können auf ganze Bytes viel schneller zugreifen als auf einzelne Bits. Das bringt mehr Leistung (Performance).
3. Wie entstehen Booleans?
Direkte Zuweisung: let an: bool = true; (Wichtig: ohne Anführungszeichen, sonst wäre es Text!).

* Vergleiche: Mathematische Prüfungen wie "kleiner als" (<) oder "größer als" (>) geben automatisch ein true oder false zurück.

* Typ-Methoden: Zahlen haben eingebaute Werkzeuge, die Fragen beantworten (z. B. .is_positive()).

📌 Merkzettel: Der Datentyp char in Rust
⚙️ Syntax im Vergleich
Rust
let buchstabe: char = 'B'; // ✅ Gültiger char (Einfache Anführungszeichen)
let emoji: char = '🎧';    // ✅ Gültiger char (Ein Emoji ist EIN Zeichen)

// let fehler: char = 'AB';  ❌ FEHLER: Mehr als ein Zeichen!
// let text: char = "B";     ❌ FEHLER: Doppelte Anführungszeichen machen daraus einen String!

Schnelle Eselsbrücke für Anfänger
Einfache Striche (') = Einsames Zeichen (char).

Doppelte Striche (") = Doppelt so viele Zeichen wie du willst (String).

Da Rust-Zahlen und -Texte vollgepackt mit Methoden sind, kannst du einen char jederzeit ganz einfach prüfen oder manipulieren!

Nächste Lektion: Projekt für Variablen und Datentypen programmieren  Übungen 